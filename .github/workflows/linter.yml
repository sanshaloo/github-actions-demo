---
name: github-actions-demo

on:
  workflow_dispatch:

jobs:
  build:
    name: Lint code base
    runs-on: ubuntu-latest
  permissions:
    contents: read
    packages: read
    statuses: write

    steps:
      - name: checkout code
        uses: actions/checkout@v3
